rules_version = '2'
service firebase.storage {
  match /b/{bucket}/o {
    
    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isAdmin() {
      return isAuthenticated() && 
             firestore.get(/databases/(default)/documents/user_profiles/$(request.auth.uid)).data.is_admin == true;
    }
    
    function isSuperAdmin() {
      return isAuthenticated() && 
             firestore.get(/databases/(default)/documents/user_profiles/$(request.auth.uid)).data.is_superadmin == true;
    }
    
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    // Custom images - user can upload their own images
    match /custom_images/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if isOwner(userId) || isAdmin();
    }

    // Custom orders - anyone can upload images for custom orders
    match /custom-orders/{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }

    // Product images - public read, admin write
    match /products/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
    match /product_images/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Banner images - public read, admin write
    match /banner_images/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Banners folder - public read, admin write
    match /banners/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Category images - public read, admin write
    match /categories/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
    match /category_images/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Posts images - public read, admin write
    match /posts/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Instagram images - public read, admin write
    match /instagram/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // General uploads - fallback (DENY by default for security)
    match /{allPaths=**} {
      allow read: if false;
      allow write: if false;
    }
  }
}
